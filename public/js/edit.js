window.addEventListener('load',function(){
     let fomularioEdit = document.getElementById('formEdit2');

     fomularioEdit.elements.name.focus();
    console.log(fomularioEdit);
     console.log(fomularioEdit.elements.name.value);
 
});
